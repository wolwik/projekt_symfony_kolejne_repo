{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create question</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit">Dodaj</button>
    {{ form_end(form) }}
{% endblock %}

